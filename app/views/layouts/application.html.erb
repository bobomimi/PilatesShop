<!DOCTYPE html>
<html lang="en">
  <head>
    <title> 
    <% if yield(:title).present? %>
        <%= yield(:title) %>
    <% else %>
        DonPilates | Pilates equipment | Pilates training 
    <% end %>
  </title>
  <% if yield(:meta_description).present? %>
    <meta name="description" content="<%= yield(:meta_description) %>" />
  <% else %>
    <meta name="description" content="Donpilates.com  pilates equipment, training and exercises for every level, videos, books and more. From Daniel Pilates." />
  <% end %>
    <% if @og_properties.present? %>
      <%= meta_og_tags @og_properties %>
    <% end %>
    <%= canonical_tag -%>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload', cache: 'cached/all' %>
    <%= metamagic %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="alternate" hreflang="en" href="http://donpilates.com/en">
    <link rel="alternate" hreflang="es" href="http://donpilates.com/es">
    <style>
      body {
       padding-top: 54px;
      }

      @media (min-width: 992px) {
       body {
        padding-top: 56px;
       }
      }

      .carousel-item {
        height: 65vh;
        min-height: 300px;
        background: no-repeat center center scroll;
       -webkit-background-size: cover;
       -moz-background-size: cover;
       -o-background-size: cover;
       background-size: cover;
      }

      .portfolio-item {
         margin-bottom: 30px;
       }

    </style>
  </head>

  <body>
  
   <!-- Navigation -->
    <nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark fixed-top">
      <div class="container">
      <%= link_to 'Daniel Pilates', root_path, class: "navbar-brand" %>
      <%= link_to url_for(locale: :en) do %>
        <%= image_tag('', width: '23', height: '15.33', alt: 'English') %>
      <% end %>
      <%= link_to url_for(locale: :es) do %>
        <%= image_tag('', width: '23', height: '15.33', alt: 'Spanish') %>
      <% end %>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
          <ul class="navbar-nav ml-auto">
            
            <li class="nav-item">
              
            </li>
            
            
            <li class="nav-item">
              <%= link_to t('navbar.blog'), posts_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to t('navbar.products'), products_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to t('navbar.about'), welcome_about_path, class: "nav-link" %>
            <li>
            <li class="nav-item">
              <%= link_to t('navbar.contact'), new_contact_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              
            </li>
            <li class="nav-item">
              
            </li>
            <li class="nav-item">
              <% if admin_signed_in? %>
                <%= link_to "Sign Out", destroy_admin_session_path, method: :delete, class: "nav-link" %>
              <% end %>
            </li>
          </ul>
          
        </div>
      </div>
    </nav>
<% if notice %>
    <p class="notice"><%= notice %></p>
  <% elsif alert %>
    <p class="alert"><%= alert %></p>
  <% end %>
    <%= yield %>
  </body>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload', cache: 'cached/all' %>
</html>